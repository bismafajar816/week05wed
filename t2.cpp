#include <iostream>
#include <cmath>
using namespace std;

main()
{
   float number1;
   
   float number1,power1;
   cout <<"enter number:";
   cin >> number1;
   cout <<"enter number:";
   cin >> power1;
   float total = max(number1,number2);
   cout<<total <<" is greater number.";
   float small = min(number1,number2);
   cout<<small <<" is smaller number.";
   int square = sqrt(number1);
   cout <<"square root is:"<<square;
   int power(number1, power1);
   cout <<"power is:"<<power;
   int cube = cbrt(3);
   cout <<"cube root :"<<cube;
   float next = ceil(3.9);
   cout <<"ceiling is:"<<next;
   int previous = floor(3.9);
   cout <<"floor:"<<previous;


}